apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: {{ include "celery-main.fullname" . }}
  namespace: {{ .Release.Namespace }}
spec:
  minAvailable: {{ .Values.replicaCount }}
  selector:
    matchLabels:
      app: {{ include "celery-main.name" . }}