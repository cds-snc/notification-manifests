apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "notify-document-download.fullname" . }}-test-connection"
  labels:
    {{- include "notify-document-download.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "notify-document-download.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
