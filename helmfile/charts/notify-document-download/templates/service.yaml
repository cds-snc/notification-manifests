apiVersion: v1
kind: Service
metadata:
  name: {{ include "notify-document-download.fullname" . }}
  labels:
    {{- include "notify-document-download.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "notify-document-download.selectorLabels" . | nindent 4 }}
