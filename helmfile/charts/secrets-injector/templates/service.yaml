apiVersion: v1
kind: Service
metadata:
  name: {{ include "secrets-injector.fullname" . }}
  labels:
    {{- include "secrets-injector.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "secrets-injector.selectorLabels" . | nindent 4 }}
