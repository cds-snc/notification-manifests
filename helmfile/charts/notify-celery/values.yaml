nodes:
  # CELERY BEAT
  beat:
    name: beat
    scriptArg: "sh /app/scripts/run_celery_beat.sh"
    deployment:
      deploymentBeatEnabled: true
      terminationGracePeriodSeconds: 30
      nodeSelector:
        eks.amazonaws.com/capacityType: ON_DEMAND
      replicas: 3
    pdb:
      pdbEnabled: false
    autoscaling:
      hpaEnabled: false
    service:
      serviceEnabled: true
  # STATIC CONFIGS
  # CELERY FOR CORE TASKS
  core-tasks-static:
    name: core-tasks-static
    scriptArg: "sh /app/scripts/run_celery_core_tasks.sh"
    deployment:
      deploymentEnabled: true
      terminationGracePeriodSeconds: 60
      resources:
        requests:
          cpu: "100m"
          memory: "800Mi"
        limits:
          cpu: "550m"
          memory: "1024Mi"
      priorityClassName: high-priority
      nodeSelector:
        eks.amazonaws.com/capacityType: ON_DEMAND
      replicas: 3
    pdb:
      pdbEnabled: true
      minAvailable: 2
    autoscaling:
      hpaEnabled: false
    service:
      serviceEnabled: true
  # CELERY FOR SENDING EMAILS
  email-send-static:
    name: email-send-static
    scriptArg: "sh /app/scripts/run_celery_send_email.sh"
    deployment:
      deploymentEnabled: true
      terminationGracePeriodSeconds: 60
      resources:
        requests:
          cpu: "100m"
          memory: "800Mi"
        limits:
          cpu: "550m"
          memory: "1024Mi"
      priorityClassName: high-priority
      nodeSelector:
        eks.amazonaws.com/capacityType: ON_DEMAND
      replicas: 3
    pdb:  
      pdbEnabled: true
      minAvailable: 2
    autoscaling:
      hpaEnabled: false
    service:
      serviceEnabled: true
  # CELERY FOR SENDING NON-DEDICATED LONG CODE SMS
  sms-send-static:
    name: sms-send-static
    scriptArg: "sh /app/scripts/run_celery_send_sms.sh"
    deployment:
      deploymentEnabled: true
      terminationGracePeriodSeconds: 60
      resources:
        requests:
          cpu: "100m"
          memory: "800Mi"
        limits:
          cpu: "550m"
          memory: "1024Mi"
      priorityClassName: high-priority
      nodeSelector:
        eks.amazonaws.com/capacityType: ON_DEMAND
      replicas: 3
    pdb:
      pdbEnabled: true
      minAvailable: 2
    autoscaling:
      hpaEnabled: false
    service:
      serviceEnabled: true
  # CELERY FOR SENDING NON-DEDICATED LONG CODE SMS WITH FAIR QUEUES
  sms-send-fair-static:
    name: sms-send-fair-static
    scriptArg: "sh /app/scripts/run_celery_send_sms_ratelimit.sh"
    deployment:
      deploymentEnabled: false
      terminationGracePeriodSeconds: 60
      resources:
        requests:
          cpu: "100m"
          memory: "800Mi"
        limits:
          cpu: "550m"
          memory: "1024Mi"
      priorityClassName: high-priority
      nodeSelector:
        eks.amazonaws.com/capacityType: ON_DEMAND
      replicas: 1
    pdb:
      pdbEnabled: false
      minAvailable: 1
    autoscaling:
      hpaEnabled: false
    service:
      serviceEnabled: true
  # CELERY FOR SENDING SMS OVER DEDICATED LONG CODE
  sms-dedicated-static:
    name: sms-dedicated-static
    scriptArg: "sh /app/scripts/run_celery_sms.sh"
    deployment:
      deploymentBeatEnabled: true
      terminationGracePeriodSeconds: 60
      nodeSelector:
        eks.amazonaws.com/capacityType: ON_DEMAND
      replicas: 3
    pdb:
      pdbEnabled: false
    autoscaling:
      hpaEnabled: false
    service:
      serviceEnabled: true
  # CELERY FOR CREATING REPORTS
  generate-reports-static:
    name: generate-reports-static
    scriptArg: "sh /app/scripts/run_celery_report_tasks.sh"
    deployment:
      deploymentEnabled: true
      terminationGracePeriodSeconds: 60
      resources:
        requests:
          cpu: "100m"
          memory: "400Mi"
        limits:
          cpu: "550m"
          memory: "1024Mi"
      priorityClassName: high-priority
      nodeSelector:
        eks.amazonaws.com/capacityType: ON_DEMAND
      replicas: 1
    pdb:
      pdbEnabled: false
      minAvailable: 1
    autoscaling:
      hpaEnabled: false
    service:
      serviceEnabled: true
  # SCALABLE CONFIGS
  # CELERY FOR CORE TASKS
  core-tasks-scalable:
    name: core-tasks-scalable
    scriptArg: "sh /app/scripts/run_celery_core_tasks.sh"
    deployment:
      deploymentEnabled: true
      priorityClassName: celery
      terminationGracePeriodSeconds: 60
      replicas: 3
      nodeSelector:
        karpenter.sh/capacity-type: spot
      resources:
        requests:
          cpu: "100m"
          memory: "800Mi"
        limits:
          cpu: "550m"
          memory: "1024Mi"
    autoscaling:
      hpaEnabled: true
      minReplicas: 3
      maxReplicas: 30
      targetCPUUtilizationPercentage: 25
      scaleUpPodsValue: 6
      scaleUpPeriodSeconds: 45
    pdb:
      pdbEnabled: true
      minAvailable: 2
    service:
      serviceEnabled: true
  # CELERY FOR DELIVERY RECEIPTS
  delivery-receipts-scalable:
    name: delivery-receipts-scalable
    scriptArg: "sh /app/scripts/run_celery_receipts.sh"
    deployment:
      deploymentEnabled: true
      priorityClassName: celery
      terminationGracePeriodSeconds: 60
      replicas: 3
      nodeSelector:
        karpenter.sh/capacity-type: spot
      resources:
        requests:
          cpu: "100m"
          memory: "800Mi"
        limits:
          cpu: "550m"
          memory: "1024Mi"
    autoscaling:
      hpaEnabled: true
      minReplicas: 3
      maxReplicas: 10
      targetCPUUtilizationPercentage: 25
      scaleUpPodsValue: 6
      scaleUpPeriodSeconds: 45
    pdb:
      pdbEnabled: true
      minAvailable: 2
    service:
      serviceEnabled: true
  # CELERY FOR SENDING EMAILS
  scalable-email:
    name: email-send-scalable
    scriptArg: "sh /app/scripts/run_celery_send_email.sh"
    deployment:
      deploymentEnabled: true
      priorityClassName: celery
      terminationGracePeriodSeconds: 60
      replicas: 3
      nodeSelector:
        karpenter.sh/capacity-type: spot
      resources:
        requests:
          cpu: "100m"
          memory: "800Mi"
        limits:
          cpu: "550m"
          memory: "1024Mi"
    autoscaling:
      hpaEnabled: true
      minReplicas: 3
      maxReplicas: 30
      targetCPUUtilizationPercentage: 25
      scaleUpPodsValue: 6
      scaleUpPeriodSeconds: 45
    pdb:
      pdbEnabled: true
      minAvailable: 2
    service:
      serviceEnabled: true
  # CELERY FOR SENDING NON-DEDICATED LONG CODE SMS
  scalable-sms:
    name: sms-send-scalable
    scriptArg: "sh /app/scripts/run_celery_send_sms.sh"
    deployment:
      deploymentEnabled: true
      priorityClassName: celery
      terminationGracePeriodSeconds: 60
      replicas: 3
      nodeSelector:
        karpenter.sh/capacity-type: spot
      resources:
        requests:
          cpu: "100m"
          memory: "800Mi"
        limits:
          cpu: "550m"
          memory: "1024Mi"
    autoscaling:
      hpaEnabled: true
      minReplicas: 3
      maxReplicas: 3
      targetCPUUtilizationPercentage: 25
      scaleUpPodsValue: 6
      scaleUpPeriodSeconds: 45
    pdb:
      pdbEnabled: true
      minAvailable: 2
    service:
      serviceEnabled: true

image:
  repository: public.ecr.aws/cds-snc/notify-api
  pullPolicy: Always
  tag: "latest"

replicaCount: 3

imagePullSecrets: []
nameOverride: ""
fullnameOverride: ""

updateStrategy:
  type: RollingUpdate
  rollingUpdate:
    maxSurge: 25%
    maxUnavailable: 25%

serviceAccount:
  create: true
  serviceAccountName: "notify-celery"

service:
  enabled: true
  port: 80
  annotations:
    service.beta.kubernetes.io/aws-load-balancer-internal: "true"

podAnnotations: {}

podSecurityContext: {}

tolerations: []

affinity: {}

dnsPolicy: ClusterFirst
restartPolicy: Always
schedulerName: default-scheduler
terminationGracePeriodSeconds: 60

secretProviderClass:
  secretname: notify-celery
  enabled: true
  provider: aws
  parameters:
    objects: |
      - objectName: INTERNAL_DNS_KEY_BASE64
        objectType: "secretsmanager"
      - objectName: INTERNAL_DNS_CERT_BASE64
        objectType: "secretsmanager"

  secretObjects:
    - data:
        - key: tls.key
          objectName: INTERNAL_DNS_KEY_BASE64
        - key: tls.crt
          objectName: INTERNAL_DNS_CERT_BASE64
      secretName: nginx
      type: kubernetes.io/tls

# Default VPA configuration for all celery nodes
# Can be overridden per-node by adding verticalPodAutoscaler config to each node
defaultVerticalPodAutoscaler:
  enabled: false
  # updateMode: Off (recommendations only), Initial (on pod creation), Auto (automatic updates)
  # Use "Off" or "Initial" when HPA is enabled to avoid conflicts
  updateMode: "Off"
  controlledResources:
    - cpu
    - memory
  minAllowed:
    cpu: "50m"
    memory: "256Mi"
  maxAllowed:
    cpu: "1000m"
    memory: "2Gi"

dockerhub:
  username: "changeme"
  pat: "changeme"