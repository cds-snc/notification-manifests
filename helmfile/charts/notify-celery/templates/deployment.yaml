{{- range $node := .Values.nodes }}
{{- with $ -}}
{{ if $node.deployment.deploymentEnabled }}
apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app: {{ include "celery.fullname" . }}-{{ $node.name }}
  name:  {{ include "celery.fullname" . }}-{{ $node.name }}
  namespace: {{ .Release.Namespace }}
spec:
  replicas: {{ $node.deployment.replicas }}
  revisionHistoryLimit: 5
  selector:
    matchLabels:
      app: {{ include "celery.fullname" . }}-{{ $node.name }}
  strategy:
    rollingUpdate:
      maxSurge: {{ .Values.updateStrategy.rollingUpdate.maxSurge }}
      maxUnavailable: {{ .Values.updateStrategy.rollingUpdate.maxUnavailable }}
    type: {{ .Values.updateStrategy.type }}
  template:
    metadata:
      {{- with .Values.podAnnotations }}
      annotations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      labels:
        app: {{ include "celery.fullname" . }}-{{ $node.name }}
    spec:
      nodeSelector: {{ $node.deployment.nodeSelector | toYaml | nindent 8 }}
      serviceAccountName: {{ .Values.serviceAccount.serviceAccountName }}
      priorityClassName: {{ $node.deployment.priorityClassName }}
      {{ if not .Values.cloudwatchAgent.enabled }}
      # Skipped when the cloudwatch-agent runs as a sidecar: it starts as a
      # regular container, so it isn't up yet by the time initContainers run.
      initContainers:
      - name: wait-cwagent-ready
        env:
          - name: STATSD_HOST
            valueFrom:
              fieldRef:
                fieldPath: spec.nodeName
        image: "{{ .Values.image.repository }}:{{ .Values.image.tag | default .Chart.AppVersion }}"
        imagePullPolicy: IfNotPresent
        command: ["/bin/sh"]
        args: ["-c", "sh /app/scripts/cwcheck.sh"]
      {{ end }}
      containers:
        - name: {{ include "celery.fullname" . }}-{{ $node.name }}
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag | default .Chart.AppVersion }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          {{ if .Values.secretProviderClass.enabled }}
          volumeMounts:
            - name: secrets-store-inline
              mountPath: "/mnt/secrets-store"
              readOnly: true  
          {{ end }}
          env:
            # Includes specific ENV Variables
            - name: STATSD_HOST
              {{ if .Values.cloudwatchAgent.enabled }}
              value: "localhost"
              {{ else }}
              valueFrom:
                fieldRef:
                  fieldPath: spec.nodeName
              {{ end }}
            # Includes common ENV Variables
            {{- range $key, $val := .Values.celeryCommon }}
            - name: {{ $key }}
              value: {{ $val | quote }}
            {{- end }}
            # Includes secret ENV Variables
            {{- range $key, $val := .Values.celerySecrets }}
            - name: {{ $key }}
              valueFrom:
                secretKeyRef:
                  name: notify-celery
                  key: {{ $key }}
            {{- end }}
          lifecycle:
            preStop:
              exec:
                command:
                - /bin/bash
                - -c
                - /app/scripts/run_celery_exit.sh
          command: ["/bin/sh"]
          args: ["-c", "{{ $node.scriptArg }}"]
          resources:
            requests:
              cpu: {{ $node.deployment.resources.requests.cpu }}
              memory: {{ $node.deployment.resources.requests.memory }}
            limits:
              cpu: {{ $node.deployment.resources.limits.cpu }}
              memory: {{ $node.deployment.resources.limits.memory }}
        {{ if .Values.cloudwatchAgent.enabled }}
        - name: cloudwatch-agent
          image: "{{ .Values.cloudwatchAgent.image.repository }}:{{ .Values.cloudwatchAgent.image.tag }}"
          env:
            # Without this, the agent can't reach the EC2 metadata service on
            # Fargate, assumes it's on-prem, and ignores the IRSA credentials.
            - name: RUN_IN_AWS
              value: "True"
          volumeMounts:
            - name: cwagentconfig
              mountPath: /etc/cwagentconfig
              readOnly: true
          resources:
            {{- toYaml .Values.cloudwatchAgent.resources | nindent 12 }}
        {{ end }}
      {{ if or .Values.secretProviderClass.enabled .Values.cloudwatchAgent.enabled }}    
      volumes:
        {{ if .Values.secretProviderClass.enabled }}
        - name: secrets-store-inline
          csi:
            driver: secrets-store.csi.k8s.io
            readOnly: true
            volumeAttributes:
              secretProviderClass: {{ include "celery.fullname" . }}
        {{ end }}
        {{ if .Values.cloudwatchAgent.enabled }}
        - name: cwagentconfig
          configMap:
            name: {{ include "celery.fullname" . }}-cwagentconfig
        {{ end }}
      {{ end }}   
      dnsPolicy: {{ .Values.dnsPolicy }}
      restartPolicy: {{ .Values.restartPolicy }}
      schedulerName: {{ .Values.schedulerName }}
      securityContext: {}
      terminationGracePeriodSeconds: {{ $node.deployment.terminationGracePeriodSeconds }}
status: {}
{{- end }}
{{- end }}
---
{{- end }}