name: Production - Helmfile Apply Specific Image
run-name: Production - Helmfile Apply Specific Image ${{ github.event.inputs.component }} - ${{ github.event.inputs.image_tag }}

on:
  workflow_dispatch:
    inputs:
      component:
        description: "Application component to rollout"
        required: true
        type: choice
        options:
          - api
          - admin
      image_tag:
        description: "Container image tag to deploy"
        required: true
        type: string

concurrency:
  group: helmfile-production-apply-specific-image-${{ github.event.inputs.component }}
  cancel-in-progress: false

env:
  GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
  HELMFILE_FILE_PATH: ${{ github.workspace }}/helmfile
  PRODUCTION_AWS_ACCOUNT: ${{ secrets.PRODUCTION_AWS_ACCOUNT_ID }}
  OP_SERVICE_ACCOUNT_TOKEN: ${{ secrets.OP_SERVICE_ACCOUNT_TOKEN_PRODUCTION }}
  ACCOUNT_ID: ${{ secrets.PRODUCTION_AWS_ACCOUNT_ID }}
  AWS_DEFAULT_REGION: ca-central-1

permissions:
  id-token: write # This is required for requesting the OIDC JWT
  contents: read

jobs:
  rollout:
    runs-on: ubuntu-latest
    name: Rollout on EKS
    steps:
      - name: Inject token authentication
        run: |
          git config --global url."https://x-access-token:${{ secrets.GITHUB_TOKEN }}@github.com/".insteadOf "https://github.com/"

      - name: Configure credentials to Notify using OIDC
        uses: aws-actions/configure-aws-credentials@ececac1a45f3b08a01d2dd070d28d111c5fe6722 # v4.1.0
        with:
          role-to-assume: arn:aws:iam::${{ env.ACCOUNT_ID }}:role/notification-manifests-helmfile-production-apply
          role-session-name: NotifyManifestsApplySpecificImage
          aws-region: ${{ env.AWS_DEFAULT_REGION }}

      - name: Checkout
        uses: actions/checkout@f43a0e5ff2bd294095638e18286ca9a3d1956744 # v3.6.0
        with:
          fetch-depth: 0

      - name: Setup helmfile
        uses: mamezou-tech/setup-helmfile@fd46979d2984c886929c416fbdf859b1c5efa0ea # v2.1.0
        with:
          install-kubectl: yes
          install-helm: yes
          helmfile-version: "v1.4.4"
          helm-s3-plugin-version: "v0.16.2"

      - name: Install OpenVPN
        run: |
          sudo apt update
          sudo apt install -y openvpn openvpn-systemd-resolved

      - name: Setup Terraform tools
        uses: cds-snc/terraform-tools-setup@v1
        env:
          CONFTEST_VERSION: 0.30.0
          TERRAFORM_VERSION: 1.9.5
          TERRAGRUNT_VERSION: 0.66.9
          TF_SUMMARIZE_VERSION: 0.2.3

      - name: Install 1Pass CLI
        run: |
          curl -o 1pass.deb https://downloads.1password.com/linux/debian/amd64/stable/1password-cli-amd64-latest.deb
          sudo dpkg -i 1pass.deb

      - name: Retrieve VPN Config
        run: |
          scripts/createVPNConfig.sh production

      - name: Connect to VPN
        uses: kota65535/github-openvpn-connect-action@cd2ed8a90cc7b060dc4e001143e811b5f7ea0af5 # v3.1.0
        with:
          config_file: /var/tmp/production.ovpn
          echo_config: false

      - name: Configure kubeconfig
        run: |
          aws eks update-kubeconfig --name notification-canada-ca-production-eks-cluster --alias production

      - name: Load Context Variables
        run: |
          ./helmfile/getContext.sh -g

      - name: Rollout selected component with specified image tag
        id: helmfile_apply
        env:
          COMPONENT: ${{ github.event.inputs.component }}
          IMAGE_TAG: ${{ github.event.inputs.image_tag }}
        run: |
          case "$COMPONENT" in
            api)
              STATE_KEY="API_DOCKER_TAG"
              ;;
            admin)
              STATE_KEY="ADMIN_DOCKER_TAG"
              ;;
            *)
              echo "Unsupported component: $COMPONENT"
              exit 1
              ;;
          esac

          pushd helmfile
          helmfile --environment production \
            -l "app=notify-${COMPONENT}" \
            --state-values-set-string "${STATE_KEY}=${IMAGE_TAG}" \
            apply
          popd

      - name: Notify Slack channel if this job failed
        if: ${{ failure() }}
        run: |
          json='{"text":"<!here> Production rollback failed for ${{ github.event.inputs.component }} (${{ github.event.inputs.image_tag }}) in <https://github.com/${{ github.repository }}/actions/runs/${{ github.run_id }}|notification-manifests>."}'
          curl -X POST -H 'Content-type: application/json' --data "$json" ${{ secrets.SLACK_WEBHOOK }}

  clear-cache:
    if: github.event.inputs.component == 'api'
    needs: rollout
    uses: ./.github/workflows/clear-notify-cache.yaml
    with:
      environment: production
      cache_clear_user_name: CACHE_CLEAR_USER
      api_url: https://api.notification.canada.ca
    secrets:
      CACHE_CLEAR_CLIENT_SECRET: ${{ secrets.PRODUCTION_CACHE_CLEAR_CLIENT_SECRET }}