name: Pond_Test_2 

on:
  workflow_run:
    workflows: ["Pond_Test_1"]
    types:
      - completed

jobs:
  test1:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Create ZIP bundle
        run: |
          echo "Job Two Complete!"