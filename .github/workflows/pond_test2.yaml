name: Pond - Test 2 

on:
  workflow_dispatch:
  workflow_run:
    workflows: ["Pond - Test 1"]
    types:
      - completed

jobs:
  test1:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Create ZIP bundle
        run: |
          echo "Job Two Complete!"