apiVersion: v1
kind: Service
metadata:
  labels:
    name: celery-sms-dedicated-static-dedicated-static
  name: celery-sms-dedicated-static
  namespace: notification-canada-ca
spec:
  type: ClusterIP
  # Putting a dummy port because we need to specify ports
  ports:
    - port: 80
      targetPort: 80
  selector:
    app: celery-sms-dedicated-static
