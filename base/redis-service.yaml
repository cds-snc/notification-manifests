apiVersion: v1
kind: Service
metadata:
  labels:
    name: redis
  name: redis
  namespace: notification-canada-ca
spec:
  type: ClusterIP
  ports:
    - port: 6379
      targetPort: 6379
  selector:
    app: redis
